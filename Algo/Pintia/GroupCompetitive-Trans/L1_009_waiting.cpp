// #数论 - 最大公约数(GCD)

#include <bits/stdc++.h>

using namespace std;

int n;
vector<string> nums;

int main()
{
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        cin >> nums[i];
    }
    for (int i = 0; i < n; i++)
    {
    }
}