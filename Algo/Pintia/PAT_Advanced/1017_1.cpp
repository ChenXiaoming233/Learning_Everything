#include <bits/stdc++.h>

using namespace std;

int n, k;
struct Cos{
    int t, p;
    int ts;
    bool operator< (const Cos& other)const{
        return ts < other.ts;
    } 
};

int main(){
    cin >> n >> k;
    vector<Cos> coss; 
    for(int i = 0; i <)
}