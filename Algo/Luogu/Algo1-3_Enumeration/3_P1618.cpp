#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#define rep(i, a, b) for(int i = a; i <= b; i++)

using namespace std;


int main() {
    vector<string> ans(10000);
    int a1, b1, c1, count = 0;
    cin >> a1 >> b1 >> c1; 
    
    for(int i = 0; i < count; i++){
        cout << ans[i] << endl;
    }

}